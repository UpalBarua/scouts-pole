import { Router } from 'express';
import mongoClient from '../db/mongoClient';

const router = new Router();

router.get('/', (req, res) => {
  // TODO - user collection - Amran
});

export default router;
